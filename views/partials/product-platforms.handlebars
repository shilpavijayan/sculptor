<div id="productplatforms" class="slide">
     {{#each platforms}}
        <div class="subitem"> 	     
          <a href={{productPlatform.link_url}} class="btn linkto"><img src={{image_src}} alt={{name}} /></a>
          <div class="itm-grp">
               <h3>{{name}}</h3>
               <h4>{{type}}</h4>
          </div>
	  <p>{{desc}}</p>
	</div>
      {{/each}} 	
</div>  